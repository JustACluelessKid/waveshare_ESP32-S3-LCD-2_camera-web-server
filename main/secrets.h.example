// secrets.h.example - example WiFi credentials
// Copy this to main/secrets.h and replace with your real values (do NOT commit).

const char *ssid = "YOUR_SSID";
const char *password = "YOUR_PASSWORD";
